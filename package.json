{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean-all": "npx lerna clean && rm -rf ./node_modules",
    "test:unit": "npx lerna run test:unit",
    "lint": "npx lerna run lint",
    "build": "npx lerna run build",
    "start": "npx lerna run start"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.2"
  }
}
